<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
             "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.skhu.mapper.ProfessorMapper">

	<select id="findAll" resultType="Professor">
		SELECT p.id, p.professorName,
		d.departmentName FROM Professor p
		inner join department d
		On
		p.departmentId = d.id
	</select>

	<select id="findOne" resultType="Professor">
		SELECT p.id, p.professorName, d.departmentName FROM Professor p
		INNER JOIN department d
		On p.departmentId = d.id
		WHERE p.departmentId = #{p.departmentId}
		
	</select>
	
	<select id="find" resultType="Professor">
		SELECT * FROM Professor p
		INNER JOIN department d
		on p.departmentId = d.id
		WHERE p.departmentId = #{p.departmentId}
		
	</select>
	<update id="update">
    UPDATE Professor SET 
      studentNumber = #{studentNumber}, 
      name = #{name}, 
      departmentId = #{departmentId}, 
      year = #{year} 
    WHERE id = #{id}
  </update>
</mapper>
